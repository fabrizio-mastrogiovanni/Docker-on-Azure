terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }
}

provider "azurerm" {
  features {}
  # Reads ARM_SUBSCRIPTION_ID from the environment — keeps your
  # subscription ID out of the repository.
}